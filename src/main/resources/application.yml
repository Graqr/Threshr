micronaut:
  application:
    name: Threshr
  http:
    services:
      redsky-api:
        url: https://redsky.target.com/
        path: redsky_aggregations/v1/web
---
threshr:
  key:
  channel:
  test:
    data:
      stores: ${TEST_STORE_FILE_DATA:nearby_stores.json}